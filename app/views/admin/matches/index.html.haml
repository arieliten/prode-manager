.block
  .secondary-navigation
    %ul.wat-cf
      %li.first.active= link_to "#{t("web-app-theme.list", :default => "List")}", admin_matches_path
      %li= link_to "#{t("web-app-theme.new", :default => "New")}", new_admin_match_path
  .content
    %h2.title #{ t("web-app-theme.all", :default => "All") } Matches
    .inner
      %table.table
        %tr
          %th.first ID
          %th= t("activerecord.attributes.admin_match.stage_id", :default => t("activerecord.labels.stage_id", :default => "Stage_id"))
          %th= t("web-app-theme.created_at", :default => "Created at")
          %th.last &nbsp;
        - @matches.each do |match|
          %tr{:class => cycle("odd", "even")}
            %td= match.id
            %td= link_to match.stage_id, admin_match_path(match)
            %td= match.created_at
            %td.last
              = link_to t("web-app-theme.show", :default => "Show"), admin_match_path(match)
              |
              = link_to t("web-app-theme.edit", :default => "Edit"), edit_admin_match_path(match)
              |
              = link_to t("web-app-theme.delete", :default => "Delete"), admin_match_path(match), :method => :delete, :confirm => "#{t("web-app-theme.confirm", :default => "Are you sure?")}"
      .actions-bar.wat-cf
        .actions
        
- content_for :sidebar, render(:partial => 'sidebar')