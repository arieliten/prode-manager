%p= "Hola <%= @resource.email %>!"

%p Su cuenta ha sido bloqueada debido a excesivas fallos al momento de intentar iniciar sesión

%p Haga click en el siguiente enlace para poder desbloquear su cuenta:

%p= link_to 'Desbloquear mi cuenta', unlock_url(@resource, :unlock_token => @resource.unlock_token)
